<header>
  <h1>{{ page_title }}</h1>
  <p class="lead">You are currently responsible for {{keyWorkers.length}} members of staff</p>
<header>

{{#if keyWorkers}}
<div class="results results-keyworker">
  {{#each keyWorkers as |keyworker| }}
    <article id="{{ keyworker.staff_id }}" style="border-bottom:1px solid #ddd;margin-bottom:11px;padding-bottom:12px;">
      <div class="row">
        <div class="col-md-10">
          <h4>{{ keyworker.given_name }} {{ keyworker.surname }}, {{keyworker.age}}, {{expand_gender keyworker.gender}}</h4>
          <ul class="list-inline">
            <li><a class="btn btn-default" href="/keyworker/{{ keyworker.staff_id }}/caseload">View case assignments</a></li>
            <li><a class="btn btn-default" href="/keyworker/{{ keyworker.staff_id }}">View details</a></li>
          </ul>
        </div>
        <div class="col-md-2">
          <ul class="list-inline">
            <li class="label label-{{#if keyworker.caseload_capacity_over}}danger{{else}}default{{/if}}">Case files: <span class="badge" title="{{keyworker.casefiles.length}} case files assigned">{{keyworker.casefiles.length}} / {{keyworker.caseload_capacity}}</span></li>
          </ul>
        </div>
      </div>

      {{#unless keyworker.casefiles.length}}
      <div class="alert alert-info"><strong>No case files assigned:</strong> This member of staff currently has no case files assigned to them</div>
      {{/unless}}
    </article>
  {{/each}}
</div>
{{else}}
<div class="alert alert-info"><strong>No key workers found:</strong> there were no staff in the key worker role found to be and supervised by you</div>
{{/if}}
